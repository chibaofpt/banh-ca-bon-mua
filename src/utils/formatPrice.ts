/**
 * formatPrice returns a string formatted as "🐟 {price} cá".
 * Used consistently across the app for all price displays.
 */
export const formatPrice = (price: number): string => `🐟 ${price} cá`;
