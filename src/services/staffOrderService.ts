import { apiClient } from '@/src/lib/api/client';

/** Tạo order mới tại quầy (status = COMPLETED ngay lập tức). */
export async function createStaffOrder() {}

/** Tìm kiếm user theo số điện thoại để gắn vào order. */
export async function lookupUserByPhone() {}

/** Tạo ghost user mới khi khách chưa có tài khoản. */
export async function createGhostUser() {}
